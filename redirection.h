#ifndef REDIRECTION_H
#define REDIRECTION_H
#include<iostream>
#include<string>
#include<vector>
#include<stdio.h>
using namespace std;

// extern bool ip_redirect;
// extern bool op_redirect;
// extern string curr_cmnd;

void inp_redirect(vector<string>);
void out_redirect(vector<string>);
void io_redirect(vector<string>);
#endif